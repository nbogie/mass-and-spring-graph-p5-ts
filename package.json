{
  "name": "p5-ts-mass-and-spring",
  "version": "1.0.0",
  "description": "Simple mass and spring simulation in p5.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w"
  },
  "devDependencies": {
    "@types/p5": "^1.4.2",
    "browser-sync": "^2.27.9",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.6.3"
  }
}
